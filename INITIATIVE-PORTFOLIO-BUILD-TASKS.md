# DEWA DTMP â€” Initiative & Programme Portfolio
# Build Task Document
**Version:** 1.1 | April 2026
**Based on:** Feature Spec v1.0 | Shell Spec v1.0 | Codebase Audit (April 2026)
**Status:** Cross-cutting tasks X-01â€“X-03, X-05 complete. Stage 1: S1-01â€“S1-04, S1-07, S1-08 complete.

---

## Decisions Log

All five decisions have been resolved. No blockers remain.

| # | Decision | Resolution | Status |
|---|---|---|---|
| D-01 | Rename â€” new marketplace name | **"Initiative & Programme Portfolio"** is the confirmed name. No rename required. X-04 is a no-op â€” remove from task list. | âœ… Resolved |
| D-02 | `InitiativeStatus` â€” add `Pending` etc. | **Option A confirmed.** Add `Pending`, `Clarification Requested`, `Rejected` to `InitiativeStatus` type. Use `addInitiative()` with `status = "Pending"` for the full submission flow. Keeps initiative state in one place. X-02 and S1-06 unblocked. | âœ… Resolved |
| D-03 | Insights â€” page route vs drawer | **Keep `SeeInsightsDrawer`, deprecate `/initiative/:id/insights` page route.** All "See Insights" CTAs open the drawer. The page route at `/marketplaces/initiative-portfolio/initiative/:id/insights` is deprecated. S1-04, S1-07, S1-10 updated below to reflect drawer-based access. See X-05 for route disposal. | âœ… Resolved |
| D-04 | Stage 2 routing â€” query params vs nested routes | **Nested routes confirmed.** `/stage2/lifecycle-management/:initiativeId` and `/stage2/lifecycle-management/:initiativeId/projects/:projectId`. S2-01, S2-02, S2-11 unblocked. | âœ… Resolved |
| D-05 | Activity log â€” derived on render vs `activity[]` field | **Option A confirmed.** Add `activity: ActivityEntry[]` to `Initiative` in the store. Every store mutation function appends to `activity[]` before writing back to localStorage. Persistence across sessions. S2-09 unblocked. Note: this is a significant store change â€” every store write function is affected. See X-02 for store work scope. | âœ… Resolved |

### Route Disposal Resolution
**X-05 confirmed:** deprecated `/initiative/:id/insights` URL removed entirely. Drawer-based access is the only supported insights entry path.

---

## Cross-Cutting Tasks

| Task ID | Title | Type | File | Description | Dependencies | Status |
|---|---|---|---|---|---|---|
| X-01 | localStorage event listener sync | Fix | `src/data/shared/lifecyclePortfolioStore.ts`, `src/pages/LifecycleManagementPage.tsx`, `src/pages/lifecycle/LCStage2Overview.tsx` | All store write functions dispatch a same-tab `storage` event, and Stage 1 / Stage 2 subscribe so changes from Stage 3 are reflected immediately without refresh. | None | âœ… Completed |
| X-02 | Store type and structure updates | Data | `src/data/shared/lifecyclePortfolioStore.ts` | (1) Add `"Pending" \| "Clarification Requested" \| "Rejected"` to `InitiativeStatus` type. (2) Add `activity: ActivityEntry[]` field to `Initiative` interface â€” `ActivityEntry`: `{ id, timestamp, actor, action, type }` where type is `"Status Change" \| "Milestone" \| "Risk" \| "Blocker" \| "Budget" \| "Service Request" \| "Submission"`. (3) Update `computeInitiativeRAG` guard to handle new statuses (`Pending`, `Clarification Requested`, `Rejected` are excluded from RAG computation). (4) Every existing store mutation function (`updateProjectRAG`, `updateMilestoneStatus`, `updateRiskStatus`, `resolveBlocker`, `updateInitiativeStatus`, `updateInitiativeProgress`, `updateProjectProgress`, `updateProjectBudgetHealth`, `updateInitiativeBudgetSpent`, `updateProjectBudgetSpent`, `updateBlockerEscalation`) appends an `ActivityEntry` to the parent initiative's `activity[]` before writing to localStorage. (5) `addInitiative()` defaults submissions to `status = "Pending"` and writes an initial `ActivityEntry` of type `"Submission"`. | D-02, D-05 | âœ… Completed |
| X-03 | Expose `strategicPriority` values from `getPortfolioSummary()` | Data | `src/data/shared/lifecyclePortfolioStore.ts`, `src/pages/LifecycleManagementPage.tsx` | `getPortfolioSummary()` returns `strategicPriorityValues: string[]`, and Stage 1 uses that dynamic set to populate the Strategic Priority filter dropdown. | None | âœ… Completed |
| ~~X-04~~ | ~~Apply rename~~ | ~~Rename~~ | â€” | **Cancelled â€” D-01 confirmed name unchanged. No action required.** | â€” | âœ… Cancelled |
| X-05 | Dispose of deprecated insights page route | Fix | `src/App.tsx`, `src/pages/lifecycle/LCInitiativeDetailPage.tsx`, `src/pages/lifecycle/LCStage2Overview.tsx` | The deprecated `/marketplaces/initiative-portfolio/initiative/:id/insights` route is removed entirely. All remaining entry points now use the drawer directly or link to the initiative detail page. Obsolete `src/pages/lifecycle/LCInsightsPage.tsx` removed. | D-03 | âœ… Completed |

---

## Stage 1 â€” Marketplace Discovery

**Route:** `/marketplaces/initiative-portfolio`
**Primary file:** `src/pages/LifecycleManagementPage.tsx`

| Task ID | Title | Type | File | Description | Dependencies | Status |
|---|---|---|---|---|---|---|
| S1-01 | Restructure `LifecycleManagementPage` as portfolio discovery view | Refactor | `src/pages/LifecycleManagementPage.tsx` | The current page renders a workspace-style "My Active Requests / My Initiatives" panel layout. The spec requires it to lead with the Portfolio Overview Panel (S1-02) and Initiative Card Grid (S1-03). Full page restructure â€” existing workspace panels are moved to Stage 2. | S1-02, S1-03, X-03 | âœ… Completed |
| S1-02 | Build Portfolio Overview Panel | New Build | `src/pages/LifecycleManagementPage.tsx` | Always-visible sticky header panel. Values sourced from `getPortfolioSummary()`. Elements: total initiatives count, active initiatives count, RAG counts (Red / Amber / Green â€” computed via `computeInitiativeRAG` across all initiatives), average EA alignment score (show "Not assessed" if null), total budget AED (comma-formatted), budget spent AED. Four simultaneous filters: Division (all 10 + "All Divisions"), Status (All / Active / Scoping / At Risk / On Hold / Completed), Type (All + 17 types), Strategic Priority (All + dynamic values from `getPortfolioSummary().strategicPriorityValues`). All four filters operate simultaneously â€” no filter resets another. Card grid updates on any filter change without page reload. | X-01, X-03 | âœ… Completed |
| S1-03 | Build Initiative Card Grid | New Build | `src/pages/LifecycleManagementPage.tsx` | Primary content area. One card per initiative, filtered by active filter state from S1-02. Card elements: name (2-line truncate + tooltip on hover), division badge, type badge, status badge (colour-coded per existing badge patterns), RAG dot + label (computed via `computeInitiativeRAG` â€” never from stored value), progress bar + % label, EA alignment score or "Not assessed" if null, strategic priority (greyed out if not set), target date (DD MMM YYYY), owner name, project count (integer), `fromPortfolio` badge ("Derived from portfolio gap" â€” shown if `fromPortfolio = true`), View Detail CTA â†’ `/marketplaces/initiative-portfolio/initiative/:id`. Empty state: "No initiatives match your filters" with reset filters CTA. | S1-02, X-01 | âœ… Completed |
| S1-04 | Refactor `LCInitiativeDetailPage` to match spec | Refactor | `src/pages/lifecycle/LCInitiativeDetailPage.tsx` | Current page has a 6-tab structure (Overview, Projects, Applications, Compliance, Products, Retirement). Spec calls for a single-view public detail page â€” remove tab structure and consolidate to one scrollable view. Elements to add or correct: (1) `fromPortfolio` badge ("Derived from portfolio gap") + link to source asset card in Asset & Capability Portfolio if `fromPortfolio = true` and `portfolioCardId` is set. (2) Linked capability domain cross-link â€” derived from initiative type â€” navigates to Asset & Capability Portfolio capability canvas view for that domain. (3) Budget field shows total only â€” no spend detail (spend is Stage 2 only). (4) "Back to portfolio" CTA â€” returns to `/marketplaces/initiative-portfolio` preserving active filter state (see S1-08). (5) "View in workspace" CTA â€” authenticated users only (`isUserAuthenticated()`) â€” routes to `/stage2/lifecycle-management/:initiativeId`. (6) **"See Insights" CTA â€” opens `SeeInsightsDrawer` (NOT a route navigation â€” D-03 resolved). Auth-gated: if unauthenticated, show login prompt with redirect-back. If authenticated but no role set, role selector triggers within drawer.** | S1-08, D-03 | âœ… Completed |
| S1-05 | Build AI.05 Initiative Recommender component | New Build | `src/components/lifecycle/AIInitiativeRecommender.tsx` | Inline card component (not a modal, not a drawer). Rendered below initiative description on initiative detail page when `eaAlignmentScore < 50` OR `status = "At Risk"`. Matching logic (prototype â€” no API call): hardcoded per initiative type and capability domain against seeded data. Returns: (a) matching initiative with highest `eaAlignmentScore` + highest `progress` â€” shows name, type, status, progress, alignment score, link to detail; or (b) if no match â€” framework suggestion based on domain type with pre-populated framework recommendation. Card title: "Related Initiative" or "Suggested Action". One-line rationale. CTA. Dismissible â€” dismissed state stored in localStorage keyed by `initiative-recommender-dismissed-{initiativeId}` per session. Also used on Asset & Capability Portfolio capability detail page as an "Initiatives addressing this gap" section â€” embedding logic handled at point of use. | S1-04 | ✅ Completed |
| S1-06 | Add Step 3 Confirmation to `ExploreStartStepper` | Refactor | `src/components/lifecycle/ExploreStartStepper.tsx` | Step 3 (Confirmation) is missing from the existing stepper. Build: generated reference number (formatted ID), initiative name as submitted, status badge = "Pending", expected response date (calculated: `submittedAt + SLA hours based on priority` â€” Critical=48h, High=72h, Medium/Low=120h â€” shown as formatted date), plain text "what happens next" statement: "Your request has been sent to the Transformation Office for review. You will be notified when a decision is made.", View Status CTA (routes to Stage 2 workspace), Back to portfolio CTA. Ensure `addInitiative()` is called with `status = "Pending"` (not `"Active"`). Submission record must be visible in Stage 3 Pending Approvals immediately via localStorage event listener (X-01). | D-02, X-02, X-01 | ✅ Completed |
| S1-07 | Align `SeeInsightsDrawer` role views with spec | Refactor | `src/components/lifecycle/SeeInsightsDrawer.tsx` | **D-03: insights access is drawer-based. This task replaces any work on `LCInsightsPage.tsx` for insights content.** Verify and fix the following per spec: **initiative-owner view** â€” milestones table must have inline status dropdown calling `updateMilestoneStatus()`, blockers list must have Escalate to TO CTA (confirmation prompt) and Resolve CTA with note field (blocked if empty, calls `resolveBlocker()`), risks list must have inline status dropdown calling `updateRiskStatus()`, overall health narrative paragraph present. **senior-stakeholder view** â€” top 3 milestones by due date only (name, status, due date â€” no inline controls), open blockers shown as integer count only (no detail list), budget at-a-glance (Total / Spent / Remaining â€” no per-project breakdown), read only throughout. **general-staff view** â€” completion % as large prominent number, planned start â†’ end date range, forecast end date with "Forecast" label only if different from planned end, division + owner simple meta row, no financial data, no risk or blocker detail, read only. | D-03 | âœ… Completed |
| S1-08 | Implement filter state persistence on back-navigation | Fix | `src/pages/LifecycleManagementPage.tsx` | "Back to portfolio" CTA on initiative detail page must restore the active filter state the user had set. Store active filters in URL search params (preferred â€” shareable) or `sessionStorage`. Ensure the card grid in S1-03 reads and reapplies them on return navigation. | S1-01 | âœ… Completed |
| S1-09 | Add auth gating with redirect-back | Fix | `src/pages/LifecycleManagementPage.tsx`, `src/components/lifecycle/SeeInsightsDrawer.tsx` | "New Initiative" button must check `isUserAuthenticated()` â€” if false, show login prompt with redirect-back to `/marketplaces/initiative-portfolio`. "See Insights" CTA (now opens drawer per D-03) must check `isUserAuthenticated()` â€” if false, show login prompt with redirect-back to the current initiative detail URL. | D-03 | ✅ Completed |
| S1-10 | Trigger `RoleSelectorModal` on first Insights access via drawer | Fix | `src/components/lifecycle/SeeInsightsDrawer.tsx` | **D-03: insights access is drawer-based, not page-routed.** On drawer open: if `dtmp.lifecycle.insightsRole` is not set in localStorage, open `RoleSelectorModal` before rendering insights content. If role is set, render role-appropriate view directly. "Change role" CTA within the drawer must re-open `RoleSelectorModal`. Modal options: Initiative Owner / Senior Stakeholder / General Staff â€” each shows role label and one-line description of what they will see. Selection stored in `dtmp.lifecycle.insightsRole`. | D-03, S1-07 | ✅ Completed |

---

## Stage 2 â€” Transactional Workspace

**Routes:** `/stage2/lifecycle-management` (My Workspace) | `/stage2/lifecycle-management/:initiativeId` (Cockpit) | `/stage2/lifecycle-management/:initiativeId/projects/:projectId` (Project Detail)
**Primary file:** `src/pages/lifecycle/LCStage2Overview.tsx`
**Note:** The current file at this route is a redirect stub. All items below are New Build unless noted. Route definitions in `src/App.tsx` must be added as part of S2-01.

| Task ID | Title | Type | File | Description | Dependencies | Status |
|---|---|---|---|---|---|---|
| S2-01 | Build My Workspace Overview and add nested routes to App.tsx | New Build | `src/pages/lifecycle/LCStage2Overview.tsx`, `src/App.tsx` | Entry view when no initiative is selected (`/stage2/lifecycle-management`). Add nested routes to `App.tsx`: `/stage2/lifecycle-management` (overview), `/stage2/lifecycle-management/:initiativeId` (cockpit), `/stage2/lifecycle-management/:initiativeId/projects/:projectId` (project detail). All three are auth-gated (see S2-15). Workspace overview elements: active initiatives summary (count + RAG breakdown for user's division â€” computed), pending actions panel (milestones due â‰¤7 days count, unresolved blockers count, SLA-at-risk service request count), SLA alerts list (requests where `now > submittedAt + slaHours` â€” sorted by breach severity â€” red badge per item), My initiatives list (all initiatives in user's division â€” name, computed RAG, progress, status â€” click â†’ `/stage2/lifecycle-management/:initiativeId`), New service request shortcut CTA (opens S2-12 form without entering an initiative), role selector dropdown in header (see S2-14). | D-04, X-01, X-02, S2-14 | âœ… Unblocked |
| S2-02 | Build Initiative Management Cockpit shell and header | New Build | `src/pages/lifecycle/LCStage2Overview.tsx` | Full-page cockpit rendered at `/stage2/lifecycle-management/:initiativeId`. Header (always visible above tabs): initiative name (editable by initiative-owner â€” calls `updateInitiativeStatus()` after name change), status badge (dropdown â€” editable by initiative-owner â€” `updateInitiativeStatus()`), RAG indicator (computed via `computeInitiativeRAG` â€” read-only â€” updates on every render), progress bar + % (editable by initiative-owner â€” `updateInitiativeProgress()`), division + type + owner meta row, strategic priority if set, budget summary strip (Total / Spent / Remaining AED â€” comma-formatted), Back to workspace CTA â†’ `/stage2/lifecycle-management`. 8-tab navigation: Overview | Projects | Milestones | Risks | Blockers | Budget | Activity | Service Requests. | D-04, X-01, X-02 | âœ… Unblocked |
| S2-03 | Build Tab 1 â€” Overview | New Build | `src/pages/lifecycle/LCStage2Overview.tsx` | Initiative description (full text), key dates (target date, planned start date, forecast end date â€” forecast only shown if different from planned end, labelled "Forecast"), EA alignment score as score/100 or "Not assessed" if null, `fromPortfolio` badge ("Derived from portfolio gap") + link to source asset card if `fromPortfolio = true` and `portfolioCardId` is set, linked projects summary (count + quick RAG strip showing one dot per project coloured by computed RAG), strategic priority full text, last updated formatted (DD MMM YYYY HH:mm). | S2-02 | ðŸ”“ Unblocked (after S2-02) |
| S2-04 | Build Tab 2 â€” Projects | New Build | `src/pages/lifecycle/LCStage2Overview.tsx` | One card per child project from `getProjects(initiativeId)`. Card elements: project name, RAG badge (colour-coded), PM name, division, progress bar + %, budget health badge (On Track / At Risk / Over Budget), budget strip (Total / Spent â€” AED comma-formatted), target date formatted (red if past), open blockers count (integer), open risks count (integer), View project CTA â†’ `/stage2/lifecycle-management/:initiativeId/projects/:projectId`. | S2-02 | ðŸ”“ Unblocked (after S2-02) |
| S2-05 | Build Tab 3 â€” Milestones | New Build | `src/pages/lifecycle/LCStage2Overview.tsx` | All milestones across all child projects (iterate `getProjects(initiativeId)` â†’ each project's `milestones[]`). Columns: project name, milestone name, status badge (Complete / In Progress / Not Started / Delayed), due date (red if status = Delayed AND past due), owner (blank if not set). Inline status dropdown for initiative-owner role only â†’ `updateMilestoneStatus(projectId, milestoneId, status)`. Filter bar: All / Complete / In Progress / Not Started / Delayed. Sort: due date ascending (default) / by project / by status. | S2-02 | ðŸ”“ Unblocked (after S2-02) |
| S2-06 | Build Tab 4 â€” Risks | New Build | `src/pages/lifecycle/LCStage2Overview.tsx` | Full risk register from `getRisksByInitiativeId(id)`. Columns: title, severity badge (Critical / High / Medium / Low â€” colour-coded), likelihood, impact, mitigation summary, owner, mitigation due date (red if past due AND `status = "Open"`), status badge. Inline status dropdown for initiative-owner role only â†’ `updateRiskStatus(projectId, riskId, status)`. Filter: All / Open / Mitigated / Accepted / Closed. Sort: severity Critical first (default) / by status. | S2-02 | ðŸ”“ Unblocked (after S2-02) |
| S2-07 | Build Tab 5 â€” Blockers | New Build | `src/pages/lifecycle/LCStage2Overview.tsx` | All blockers from `getBlockersByInitiativeId(id)`. Columns: title, raised by, date raised (formatted), what is needed, escalation status badge (Not Escalated / Escalated to TO / Escalated to Division Head â€” colour-coded), resolved badge (Yes / No), TO acknowledged status (shown if `acknowledgedByTO` is set). Actions available to initiative-owner role only: **Escalate to TO** â€” available if `escalationStatus = "Not Escalated"` â€” confirmation prompt â€” calls `updateBlockerEscalation(projectId, blockerId, "Escalated to TO")` â€” record immediately visible in Stage 3 Escalations via localStorage event (X-01). **Escalate to Division Head** â€” available if `escalationStatus = "Escalated to TO"` â€” confirmation prompt â€” calls `updateBlockerEscalation(projectId, blockerId, "Escalated to Division Head")` â€” triggers Red RAG on initiative via `computeInitiativeRAG` on next render. **Resolve** â€” available at any escalation level â€” opens `resolvedNote` text field â€” resolve action blocked if field is empty â€” calls `resolveBlocker(projectId, blockerId, resolvedNote)`. | S2-02, X-01 | ðŸ”“ Unblocked (after S2-02) |
| S2-08 | Build Tab 6 â€” Budget | New Build | `src/pages/lifecycle/LCStage2Overview.tsx` | Initiative budget summary: Total / Spent / Remaining as large figures (AED comma-formatted). Budget health indicator computed inline: Remaining > 20% of Total = On Track, 10â€“20% = At Risk, <10% = Over Budget â€” colour-coded badge. Per-project budget table: project name, total budget, spent, remaining, budget health badge per row. Budget spend bar chart: total vs spent per project (seeded data â€” simple horizontal bars). Budget notes: free text field per initiative â€” initiative-owner can add/edit. `updateInitiativeBudgetSpent()` and `updateProjectBudgetSpent()` callable by initiative-owner from spend input fields. | S2-02 | ðŸ”“ Unblocked (after S2-02) |
| S2-09 | Build Tab 7 â€” Activity | New Build | `src/pages/lifecycle/LCStage2Overview.tsx` | Chronological log sourced from `initiative.activity[]` (added to store in X-02). Columns: timestamp (DD MMM YYYY HH:mm), actor (user or "System"), action (plain English â€” e.g. "Milestone 'Grid Integration Assessment' marked Complete by Eng. Khalid Al Rashidi"), type badge (Status Change / Milestone / Risk / Blocker / Budget / Service Request / Submission). Sorted most recent first. Paginated or scrollable if list is long. | S2-02, X-02, D-05 | âœ… Unblocked |
| S2-10 | Build Tab 8 â€” Service Requests | New Build | `src/pages/lifecycle/LCStage2Overview.tsx` | All service requests for this initiative (filtered from store by `initiativeId`). Columns: request type badge, submitted date (formatted + "X days ago"), SLA deadline (computed: `submittedAt + slaHours` â€” red badge if `now > deadline`), status badge, assignee ("Unassigned" if none). View detail CTA: expands row inline to show full request description and TO response notes. New service request CTA opens S2-12 form pre-populated with current initiative context. | S2-02, S2-12, X-01 | ðŸ”“ Unblocked (after S2-02, S2-12) |
| S2-11 | Build Project Detail View | New Build | `src/pages/lifecycle/LCStage2Overview.tsx` | Rendered at `/stage2/lifecycle-management/:initiativeId/projects/:projectId`. Header: project name, RAG badge (editable by initiative-owner â†’ `updateProjectRAG(projectId, rag)` â€” confirmation prompt), PM name + division, target date (red if past), progress bar + % (editable â†’ `updateProjectProgress()`), budget health badge (editable â†’ `updateProjectBudgetHealth()`), budget strip (Total / Spent â€” AED), Back to initiative CTA â†’ `/stage2/lifecycle-management/:initiativeId`. Three sections below header: Milestones (same table structure as Tab 3 but scoped to this project's `milestones[]`), Risks (same structure as Tab 4 scoped to this project), Blockers (same structure as Tab 5 with all escalation and resolve actions). | D-04, S2-04, S2-05, S2-06, S2-07 | âœ… Unblocked |
| S2-12 | Build Service Request Submission Form | New Build | `src/components/lifecycle/ServiceRequestForm.tsx` | Reusable modal or inline form component. Fields: service type dropdown (19 `LCServiceType` values â€” required), SLA indicator (auto-populated on type selection â€” shows expected delivery date calculated as `today + slaHours` â€” not editable), linked initiative (pre-populated from current initiative context if accessed from cockpit â€” editable dropdown if accessed from My Workspace shortcut â€” required), description text area (what is needed and why â€” required), priority dropdown (Standard / Urgent â€” required), supporting context text area (optional). Submit: validates all required fields â€” creates record via `addLCRequest()` with `status = "Submitted"` â€” record immediately visible in Stage 3 Service Requests via localStorage event (X-01). Cancel: closes form without saving. | X-01, X-02 | ðŸ”“ Unblocked |
| S2-13 | Build Blocker Escalation Detail panel | New Build | `src/components/lifecycle/BlockerEscalationDetail.tsx` | Reusable inline panel (not a modal). Used within Tab 5 and S2-11 blockers section. Elements: blocker title + raised by + date raised (header), what is needed (full text), current escalation status badge, TO acknowledgement status â€” if `acknowledgedByTO` is set shows acknowledged date and acknowledging TO member name. Actions: Escalate to TO CTA (if `escalationStatus = "Not Escalated"` â€” confirmation prompt), Escalate to Division Head CTA (if `escalationStatus = "Escalated to TO"` â€” confirmation prompt), Resolve CTA (available at any escalation level â€” opens `resolvedNote` text field â€” blocked if field is empty â€” calls `resolveBlocker()`). Resolution note shown when `resolved = true`. | S2-07 | ðŸ”“ Unblocked |
| S2-14 | Build Role Switching in Stage 2 header | New Build | `src/pages/lifecycle/LCStage2Overview.tsx` | Role selector dropdown always visible in workspace header when authenticated. Options: Initiative Owner / Senior Stakeholder / General Staff. On selection: writes to `dtmp.lifecycle.insightsRole` localStorage key â€” triggers immediate re-render of all role-dependent cockpit content (Tab content depth, inline update controls visibility). Current role displayed as "Viewing as: [Role Label]" in header. | None | ðŸ”“ Unblocked |
| S2-15 | Add auth guard to Stage 2 routes | Fix | `src/App.tsx` | All three Stage 2 routes (`/stage2/lifecycle-management`, `/stage2/lifecycle-management/:initiativeId`, `/stage2/lifecycle-management/:initiativeId/projects/:projectId`) must require `isUserAuthenticated() === true`. Unauthenticated users redirect to login with the attempted URL stored for redirect-back after auth. Currently no auth guard exists on this route. | None | ðŸ”“ Unblocked |

---

## Stage 3 â€” Transformation Office Console

**Route:** `/stage3/lifecycle-management`
**Primary file:** `src/pages/lifecycle/LCStage3Page.tsx`
**Note:** Stage 3 is largely built. All tasks below are targeted refactors.

| Task ID | Title | Type | File | Description | Dependencies | Status |
|---|---|---|---|---|---|---|
| S3-01 | Refactor Stage 3 dashboard header metrics | Refactor | `src/pages/lifecycle/LCStage3Page.tsx` | Align all header KPI counts to exact spec definitions. Service request SLA counts: **On Track** = `now < submittedAt + slaHours`; **At Risk** = `now > (submittedAt + slaHours) Ã— 0.8` AND `now < submittedAt + slaHours`; **Breached** = `now > submittedAt + slaHours`. Open escalations: count of blockers where `resolved = false` AND `escalationStatus â‰  "Not Escalated"`. Active programmes RAG: computed via `computeInitiativeRAG` across all initiatives with `status = "Active"` or `"At Risk"` â€” never from stored values. All counts computed on every render â€” no caching. | X-01, X-02 | âœ… Unblocked |
| S3-02 | Refactor AI.06 T-Office Copilot panel | Refactor | `src/pages/lifecycle/LCStage3Page.tsx` | Collapsible panel in dashboard header. Expanded by default. Collapse/expand state persists in localStorage key `dtmp.stage3.copilotCollapsed` per session. **Queue summary text** (live computed on render): "[X] initiatives awaiting approval â€” oldest submitted [Y days] ago. [Z] service requests open: [A] on track, [B] at SLA risk, [C] breached. [D] open escalations â€” [E] critical." **SLA risk alert cards** (up to 3): hardcoded demo logic against specific seeded requests â€” card shows request type, initiative name, hours remaining or hours overdue. **Assignment suggestions**: for each unassigned service request row in the Service Requests tab, show "Suggested: [Name]" inline with Accept suggestion CTA â€” hardcoded workload logic against seeded TO team member data. **Pattern flags**: if â‰¥3 requests of the same `serviceType` from the same division are open simultaneously â€” surface alert card: "Multiple [type] requests from [division] â€” possible systemic gap." All Copilot outputs are read-only â€” no actions are taken from Copilot directly. | S3-01, X-01 | ðŸ”“ Unblocked (after S3-01) |
| S3-03 | Refactor Pending Approvals tab | Refactor | `src/pages/lifecycle/LCStage3Page.tsx` | Add to action panel on row expand: (1) **Portfolio context block** â€” visible if `fromPortfolio = true`: source asset card name, `portfolioCardId`, asset condition and recommendation pulled from portfolio store. (2) **Framework detail panel** â€” selected framework name, type, methodology, complexity from `src/data/lifecycle/frameworkCards.ts`. (3) **Approval history** â€” all prior TO notes and status changes on this submission (sourced from `activity[]` on the initiative record added in X-02). Add **status filter**: All / Pending / Clarification Requested / Escalated. Add **sort**: oldest first (default) / by priority / by division. Verify **Reject CTA** is blocked if rejection reason field is empty â€” if not currently enforced, add guard. | X-01, X-02 | âœ… Unblocked |
| S3-04 | Refactor Active Programmes tab | Refactor | `src/pages/lifecycle/LCStage3Page.tsx` | Add to initiative health card: **top risk field** (highest severity open risk across all child projects â€” show title + severity badge â€” "None" if no open risks), **open blockers count** (integer â€” rendered in red if any blocker has `escalationStatus = "Escalated to Division Head"`). Add TO-only actions to expanded initiative detail view: (1) **Update initiative status** â€” status dropdown with mandatory TO note field (blocked if note empty) â€” calls `updateInitiativeStatus()` and appends TO note to `activity[]`. (2) **Add TO observation** â€” free text note field â€” on submit: appends `ActivityEntry` of type `"Status Change"` with actor = current TO user to `initiative.activity[]`. (3) **Flag for escalation** â€” marks initiative for senior TO attention â€” sets a `flaggedForEscalation` boolean (add to `Initiative` type if not present). (4) **View full audit trail** â€” renders all `initiative.activity[]` entries in a scrollable panel. Add filters to tab header: Division dropdown + RAG dropdown (All / Red / Amber / Green) + Sort dropdown (RAG Red first / Division / Target date). | X-01, X-02, D-05 | âœ… Unblocked |
| S3-05 | Refactor Service Requests tab | Refactor | `src/pages/lifecycle/LCStage3Page.tsx` | Add: **filter by division** dropdown. Add: **filter by SLA status** dropdown (On Track / At Risk / Breached â€” using same thresholds as S3-01). **Assignee cell**: render in red if request is SLA at-risk or breached AND assignee is "Unassigned". **Deliver action**: delivery note field (required â€” block if empty) + attach output reference (text input for link or file name). Add **Internal TO notes** free-text field per request (visible to TO staff only â€” not surfaced in Stage 2 request tracking view). Add **action history** per request row (all prior status changes with timestamps and TO member names â€” sourced from service request's own action log or `activity[]`). | X-01 | ðŸ”“ Unblocked |
| S3-06 | Refactor Escalations tab | Refactor | `src/pages/lifecycle/LCStage3Page.tsx` | Add: **filter by severity** dropdown (All / Critical / High / Medium / Low). Add: **filter by status** dropdown (All / Open / Acknowledged / Resolved). **Resolve CTA**: verify blocked if `resolvedNote` field is empty â€” add guard if not currently enforced. Add **TO internal notes** free-text field per escalation item (not visible to initiative owner). Add **action history** per escalation item (all prior acknowledgement, resolution, and escalation actions with timestamps and TO member names). | X-01 | ðŸ”“ Unblocked |

---

## Task Summary

| Stage | New Build | Refactor | Fix | Data | Rename | Total |
|---|---|---|---|---|---|---|
| Cross-cutting | â€” | â€” | 1 | 2 | ~~1~~ 0 | **3** (+ 1 pending) |
| Stage 1 | 3 | 4 | 3 | â€” | â€” | **10** |
| Stage 2 | 13 | â€” | 2 | â€” | â€” | **15** |
| Stage 3 | â€” | 6 | â€” | â€” | â€” | **6** |
| **Total** | **16** | **10** | **6** | **2** | **0** | **34** |

---

## Recommended Build Order

| Step | Tasks | Notes |
|---|---|---|
| 1 | X-01, X-02, X-03, X-05 | Cross-cutting store and route changes first â€” everything downstream reads from these |
| 2 | S1-01, S1-02, S1-03 | Stage 1 restructure â€” sequential |
| 3 | S1-04, S1-05, S1-06 | Initiative detail + AI recommender + stepper Step 3 |
| 4 | S1-07, S1-08, S1-09, S1-10 | Drawer insights + filter persistence + auth gating |
| 5 | S2-15, S2-14, S2-01 | Auth guard + role switcher + workspace overview shell |
| 6 | S2-02 | Cockpit shell + header (prerequisite for all tabs) |
| 7 | S2-03 â†’ S2-10 | Tabs 1â€“8 in order |
| 8 | S2-11, S2-12, S2-13 | Project detail + service request form + blocker panel |
| 9 | S3-01 | Stage 3 header metrics (prerequisite for Copilot) |
| 10 | S3-02 â†’ S3-06 | Stage 3 tabs in order |

---

*End of Build Task Document â€” Initiative & Programme Portfolio â€” DEWA DTMP v1.1*
*Decisions resolved: D-01 through D-05.*
*For review: Alwaleed, Bilal*
*Authored: Mercy Wangari | DigitalQatalyst*
*Date: April 2026*




